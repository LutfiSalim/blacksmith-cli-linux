name: Upload Package

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Upload package
        uses: actions/upload-artifact@v3
        with:
          name: package
          path: blacksmith-cli-amd64.deb
